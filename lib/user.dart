class User {
  String name, email, phone, radius, inasis, datereg;

  User(
      {this.name,
      this.email,
      this.phone,
      this.radius,
      this.inasis,
      this.datereg});

  // Stream<UnmodifiableListView<ArticleSearch>> get articles => null;
}
